query Application($timestamp: String) {
  application(timestamp: $timestamp) {
    code
    clientVersion
    page {
      code
    }
    searchPage {
      code
    }
    gamesPage {
      code
    }
    livestreamEnabled
  }
}
